# Optional: copy to .env.local (ignored) and configure one model-access route.
# A logged-in local Codex/Claude CLI can instead use --model-auth local.
# AI_GATEWAY_API_KEY=
# For direct access, use --model-auth direct --ai-provider openai
# --ai-api-key-env DEEPSEC_OPENAI_API_KEY during setup.
# DEEPSEC_OPENAI_API_KEY=
# Never put the application's profile keys or imported subscription auth here.
